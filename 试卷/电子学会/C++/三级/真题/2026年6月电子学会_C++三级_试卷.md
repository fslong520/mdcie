# 【电子学会】 C++等级考试3级 2026年06月 真题

> 来源：www.cierobot.com | 客观题 20 道（单选 10×2 分 + 判断 10×2 分 = 40 分） + 编程题 2 道（30 分/题 = 60 分），总分 100 分

---

## 一、单选题（10 题 × 2 分 = 20 分）

### 第1题 函数声明

以下关于C++函数声明的说法，正确的是？（ ）

- A. 函数声明必须写在函数调用之后
- B. 函数声明可以只写函数名和返回值类型，不写参数列表
- C. 函数声明的作用是告诉编译器函数的名称、返回值类型和参数信息
- D. 函数声明和函数定义作用完全相同

**答案：C**

**解析：** 函数声明作用是告诉编译器函数名、返回值、参数列表，声明必须放在调用前，A错误；
缺少参数列表的声明，编译器不能识别函数信息，B错误；
定义才是函数具体实现，二者作用不同，D错误。

---

### 第2题 函数调用

函数定义为 double calc(double a, double b)，以下调用方式正确的是？（ ）

- A. calc(5);
- B. calc(3.14, 2.5);
- C. calc("3", "5");
- D. double calc(2, 3);

**答案：B**

**解析：** 该函数需要两个double类型实参；
A仅传入一个参数，参数数量不匹配；
C传入字符串，类型不匹配；
D调用前不能加返回值类型。

---

### 第3题 参数传递

以下函数参数传递方式中，函数内对形参的修改会影响实参的是？（ ）

- A. 值传递
- B. 引用传递
- C. 常量传递
- D. 数值传递

**答案：B**

**解析：** 值传递会复制变量副本，修改形参不影响外部实参；
引用传递直接绑定原变量内存地址，修改形参会同步改变实参。

---

### 第4题 引用传递

执行以下代码，输出结果是？（ ）

```cpp
#include <iostream>
using namespace std;
void change(int &x) {
    x = x * 2;
}
int main() {
    int a = 8;
    change(a);
    cout << a;
    return 0;
}
```

- A. 4
- B. 8
- C. 16
- D. 0

**答案：C**

**解析：** 函数参数为 int& 引用传递，函数内修改x会影响实参a，x=8*2=16，原变量a的值也变为16。

---

### 第5题 递归累加

以下递归函数用于计算 1~n 的累加和，横线处应填入的代码是？（ ）

```cpp
int sum(int n) {
    if(n == 1)
        return 1;
    _____________
}
```

- A. return sum(n-1);
- B. return n + sum(n-1);
- C. return n * sum(n-1);
- D. return sum(n) + n;

**答案：B**

**解析：** 递归思路：当前数字 n 加上 1~n-1 的累加和，递归为：n + sum(n - 1)。

---

### 第6题 字符串拼接strcat

以下哪个函数可以将两个 C 风格字符串拼接在一起？（ ）

- A. strcat()
- B. strcpy()
- C. strlen()
- D. strcmp()

**答案：A**

**解析：** strcat用于拼接两个字符数组字符串；
strcpy为字符串拷贝；strlen计算字符串长度；strcmp用于比较两个字符串大小。

---

### 第7题 字符串比较strcmp

执行 strcmp("test", "test")，返回值是？（ ）

- A. 大于0
- B. 等于0
- C. 小于0
- D. 不确定

**答案：B**

**解析：** strcmp 逐字符对比字符串，两个字符串完全一致时返回 0；
第一个字符串更大返回正数，更小则返回负数。

---

### 第8题 枚举法

要找出 1000 以内所有的素数，最适合使用的算法思想是？（ ）

- A. 递归法
- B. 枚举法
- C. 分治法
- D. 二分法

**答案：B**

**解析：** 枚举法遍历 2~1000 所有数字，逐个判断是否为素数。

---

### 第9题 素数统计

运行程序，输出结果为？（ ）

```cpp
#include <iostream>
using namespace std;
int main() {
    int cnt = 0;
    for(int n = 2; n<=100; n++) {
        bool isPrime = true;
        for(int i = 2; i*i<=n; i++) {
            if(n % i == 0) {
                isPrime = false;
                break;
            }
        }
        if(isPrime) cnt++;
    }
    cout<<cnt;
    return 0;
}
```

- A. 23
- B. 25
- C. 27
- D. 29

**答案：B**

**解析：** 代码遍历 2 到 100，判断每个数字是否为素数并计数，100 以内一共有 25 个素数。

---

### 第10题 逻辑与运算

以下代码用于统计 1~200 中，能同时被 3 和 5 整除的数的个数，横线处应填入的代码是？（ ）

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int cnt = 0;
    for(int i=1; i<=200; i++) {
        if( _____________ )
            cnt++;
    }
    cout << cnt;
    return 0;
}
```

- A. i % 3 == 0 || i % 5 == 0
- B. i % 3 == 0 && i % 5 == 0
- C. i / 3 == 0 && i / 5 == 0
- D. i % 15 != 0

**答案：B**

**解析：** 同时被3、5整除需要两个取模条件同时成立，使用逻辑与&&，即：i % 3 == 0 && i % 5 == 0。

---

## 二、判断题（10 题 × 2 分 = 20 分）

### 第11题 return语句

函数的返回值类型为 void 时，函数内可以使用 return; 语句提前结束函数。（ ）

**答案：正确**

**解析：** 函数为 void 类型，允许单独书写 return;，作用是函数直接返回。

---

### 第12题 函数重载

C++中，两个同名函数只要返回值类型不同，就可以构成函数重载。（ ）

**答案：错误**

**解析：** 函数重载仅依靠参数列表区分（参数个数、类型、顺序），仅返回值不同无法区分重载，编译器会报错。

---

### 第13题 值传递

值传递方式下，函数会为形参创建新的内存空间，实参和形参互不影响。（ ）

**答案：正确**

**解析：** 值传递的形参会复制实参的副本，原始实参变量不受任何影响。

---

### 第14题 递归终止条件

递归函数可以没有终止条件，编译器会自动处理。（ ）

**答案：错误**

**解析：** 递归必须设置终止条件，若无终止条件会无限递归调用，造成栈溢出程序崩溃，编译器不会自动处理。

---

### 第15题 递归与非递归

有些算法既有递归实现也有非递归实现。（ ）

**答案：正确**

**解析：** 例如阶乘、斐波那契数列等，既可以使用递归写法，也可以使用递推等实现。

---

### 第16题 strlen长度

strlen() 函数功能是计算字符串长度。（ ）

**答案：正确**

**解析：** strlen 是C风格计算字符串长度的函数。

---

### 第17题 string拼接

C++ 的 string 类可以直接用 + 运算符拼接。（ ）

**答案：正确**

**解析：** string重载了加号运算符，支持两个string对象直接相加拼接字符串。

---

### 第18题 枚举法思想

枚举法的核心是不遗漏地遍历所有可能的情况。（ ）

**答案：正确**

**解析：** 枚举法通过循环遍历问题全部候选值，逐一验证条件，保证不会漏掉符合要求的解。

---

### 第19题 枚举效率

枚举要注意程序的运行效率，否则很容易在规定的时间内得不到希望的结论。（ ）

**答案：正确**

**解析：** 当数据规模较大时，循环次数过多会超时，需要优化程序或使用其它算法。

---

### 第20题 模拟法

当问题与游戏规则、物理过程等实际场景有关时，可以考虑使用模拟法解决问题。（ ）

**答案：正确**

**解析：** 正确，模拟法按照给定规则一步步复现过程。

---

## 三、编程题（2 题 × 30 分 = 60 分）

### 第21题 画作（30分）

**OJ 链接：** <https://fslong.iok.la/problem/CIE202606L3A>

**题目描述**

墙上，挂着一幅由 n×n 个彩色方格组成的画作。画作的内容可以用一个 n×n 的字符矩阵来表示。
现在想要将这幅画顺时针旋转 90 度后重新悬挂，请你帮他计算出旋转后的画作是什么样的。

**输入格式**

第一行，一个整数表示n。
接下来 n 行，每行 n 个字符。

**输出格式**

输出 n 行，每行 n 个字符，表示顺时针旋转 90 度后的矩阵。

**样例输入 1**

```
4
ooxx
xoox
xxxx
xxxx
```

**样例输出 1**

```
xxxo
xxoo
xxox
xxxx
```

**样例输入 2**

```
2
12
34
```

**样例输出 2**

```
31
42
```

**数据范围**

1 ≤ n ≤ 1000。

**参考程序**

```cpp
#include <bits/stdc++.h>
using namespace std;
string a[1000], b[1000];
int main() {
    int n;
    cin >> n;
    for(int i=0; i<n; i++) {
        cin >> a[i];
        b[i] = a[i];  //保存旋转后的矩阵
    }
    //顺时针旋转 90°：a[i][j]=>b[j][n-1-i]
    for(int i=0; i<n; i++) {
        for(int j=0; j<n; j++) {
            b[j][n-1-i] = a[i][j];
        }
    }
    for(int i=0; i<n; i++) {
        cout << b[i] << endl;
    }
    return 0;
}
```

---

### 第22题 花坛（30分）

**OJ 链接：** <https://fslong.iok.la/problem/CIE202606L3B>

**题目描述**

花坛里有 n 朵花，每朵花的编号为 1, 2, …, n。初始时，所有花的高度都是 0。
现在给定一个数列 h = {h1, h2, …, hn}，你可以重复进行如下的浇水操作，每次操作：任选一段连续区间 [l, r]，区间内每朵花高度加 1。可以无限重复该操作，使得第 k 朵花的最终高度恰好为 hk（1≤k≤n）。
求完成目标需要的最小操作浇水次数。

**输入格式**

第一行一个整数，表示花的数量 n；
第二行 n 个整数，依次表示每朵花需要达到的目标高度 h1, h2, …, hn。

**输出格式**

输出一个整数，表示达成目标所需的最少浇水操作次数。

**样例输入 1**

```
5
3 1 2 3 1
```

**样例输出 1**

```
5
```

**样例输入 2**

```
8
4 23 75 0 23 96 50 100
```

**样例输出 2**

```
221
```

**数据范围**

1 ≤ n ≤ 100，0 ≤ hi ≤ 100，所有输入均为整数。

**参考程序**

```cpp
#include <bits/stdc++.h>
using namespace std;
int h[100];
int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> h[i];
    }
    //若后面高于前面的， 需要增加高度差的次数
    //若后面低于前面， 不需要增加次数
    int ans = h[0];
    for (int i = 1; i < n; i++) {
        int d = h[i] - h[i - 1];
        if(d > 0)
            ans += d;
    }
    cout << ans << endl;
    return 0;
}
```

---
