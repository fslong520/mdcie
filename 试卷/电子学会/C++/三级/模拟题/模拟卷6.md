---
title: "2026 年 10 月 C/C++ 三级 模拟卷6"
description: 2026年10月 电子学会青少年软件编程等级考试 C/C++ 三级 模拟卷6（单选10×2分 + 判断10×2分 = 40分 + 编程4×15分 = 60分）
---

<div class="water">

---

# 📘 2026 年 10 月 C/C++ 三级 模拟卷6

---

# 客观题 20 道（单选 10×2 分 + 判断 10×2 分 = 40 分） + 编程题 4 道（15 分/题 = 60 分）

---

### 第 1 题（电子学会三级模拟·函数基础）

一个无返回值、无参数的函数 f 定义后，在 main 函数中调用它的正确语句是？

- A. int f;
- B. f();
- C. f;
- D. return f();

---

### 第 2 题（电子学会三级模拟·函数基础）

函数定义时，函数名后面括号里声明的变量，通常称为？

- A. 形参
- B. 实参
- C. 返回值
- D. 全局变量

---

### 第 3 题（电子学会三级模拟·参数传递）

有如下代码：
```cpp
void change(int x) { x = x + 10; }
int main() {
    int a = 8;
    change(a);
    cout << a;
    return 0;
}
```
输出结果是？

- A. 18
- B. 0
- C. 8
- D. 10

---

### 第 4 题（电子学会三级模拟·递归）

有递归函数：
```cpp
int f(int n) {
    if (n == 0 || n == 1) return 1;
    return 2 * f(n - 1) + f(n - 2);
}
```
f(3) 的返回值是？

- A. 5
- B. 6
- C. 9
- D. 7

---

### 第 5 题（电子学会三级模拟·递归）

递归编程思想的核心是？

- A. 在函数内部使用循环
- B. 函数直接或间接地调用自身
- C. 用全局变量保存中间结果
- D. 用 goto 语句跳转

---

### 第 6 题（电子学会三级模拟·字符数组）

执行 `strcpy(a, b);` 之后，下列说法正确的是？

- A. 比较 a 与 b 的字典序大小
- B. 求出 b 的字符长度
- C. 把 b 的内容复制到 a 中
- D. 把 a 接到 b 的后面

---

### 第 7 题（电子学会三级模拟·数学库函数）

设 double 型变量 x = 3.7，则 `cout << (int)x + 1` 的输出是？

- A. 4
- B. 3
- C. 4.7
- D. 5

---

### 第 8 题（电子学会三级模拟·模拟）

执行以下代码，输出结果是？
```cpp
int cnt = 0;
for (int i = 1; i <= 100; i++)
    if (i % 7 == 0 || i % 11 == 0) cnt++;
cout << cnt;
```

- A. 21
- B. 23
- C. 20
- D. 22

---

### 第 9 题（电子学会三级模拟·枚举）

用枚举法统计 1~200 中能被 6 整除的数的个数，结果是？

- A. 32
- B. 33
- C. 34
- D. 36

---

### 第 10 题（电子学会三级模拟·枚举）

下列问题中，更适合用枚举法解决的是？

- A. 求一个整数序列的最大值
- B. 模拟一个报数游戏
- C. 在 1~1000 内找出所有满足某性质的数
- D. 计算两点之间的直线距离

---

### 第 11 题（电子学会三级模拟·函数基础）

一个函数可以定义在 main 函数之前，也可以定义在其后（只要在调用前有声明）。

- T. 正确
- F. 错误

---

### 第 12 题（电子学会三级模拟·函数基础）

函数定义的参数列表中，参数类型可以省略不写，默认为 int。

- T. 正确
- F. 错误

---

### 第 13 题（电子学会三级模拟·参数传递）

值传递时，实参与形参占用不同的内存空间，函数内修改形参不影响实参。

- T. 正确
- F. 错误

---

### 第 14 题（电子学会三级模拟·递归）

每次递归调用时，系统都会为本次调用分配独立的内存空间（用于保存本次调用的参数与局部变量）。

- T. 正确
- F. 错误

---

### 第 15 题（电子学会三级模拟·递归）

递归函数中的递归调用语句，必须写在函数体的最后一行。

- T. 正确
- F. 错误

---

### 第 16 题（电子学会三级模拟·字符数组）

用 `==` 直接比较两个字符数组，可以判断它们的内容是否相同。

- T. 正确
- F. 错误

---

### 第 17 题（电子学会三级模拟·字符数组）

`strcmp` 函数按字符的 ASCII 码大小，逐个比较两个字符串。

- T. 正确
- F. 错误

---

### 第 18 题（电子学会三级模拟·枚举）

枚举法的时间复杂度通常与候选空间的大小成正比。

- T. 正确
- F. 错误

---

### 第 19 题（电子学会三级模拟·模拟）

用模拟法解题时，不必关心程序的运行时间，过程模拟得越复杂越好。

- T. 正确
- F. 错误

---

### 第 20 题（电子学会三级模拟·枚举）

在枚举过程中，通过剪枝（提前排除明显不满足条件的候选值）可以提高效率。

- T. 正确
- F. 错误

---

# 编程题 4 道（在 OJ 上完成）

### 第 21 题 字母频率统计

**OJ 链接：** <https://fslong.iok.la/problem/CIE202608L3T21>

---

### 第 22 题 单词排序

**OJ 链接：** <https://fslong.iok.la/problem/CIE202608L3T22>

---

### 第 23 题 机器人漫步

**OJ 链接：** <https://fslong.iok.la/problem/CIE202608L3T23>

---

### 第 24 题 数位和统计

**OJ 链接：** <https://fslong.iok.la/problem/CIE202608L3T24>

---

</div>
